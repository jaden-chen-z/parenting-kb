{
  "name": "parenting-kb",
  "private": true,
  "version": "1.0.0",
  "description": "中文育儿知识库 + Cloudflare Pages 静态前端",
  "scripts": {
    "build": "babel '50-frontend/*.jsx' --out-dir 50-frontend --out-file-extension .js --presets=@babel/preset-react",
    "build:watch": "babel '50-frontend/*.jsx' --out-dir 50-frontend --out-file-extension .js --presets=@babel/preset-react --watch"
  },
  "devDependencies": {
    "@babel/cli": "^7.25.0",
    "@babel/core": "^7.25.0",
    "@babel/preset-react": "^7.24.7"
  }
}
